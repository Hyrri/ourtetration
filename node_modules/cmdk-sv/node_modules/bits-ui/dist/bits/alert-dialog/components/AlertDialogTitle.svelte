<script>import { melt } from "@melt-ui/svelte";
import { getCtx, getAttrs } from "../ctx.js";
export let level = "h2";
export let asChild = false;
export let id = void 0;
const {
  elements: { title },
  ids
} = getCtx();
$:
  if (id) {
    ids.title.set(id);
  }
$:
  builder = $title;
const attrs = getAttrs("title");
</script>

{#if asChild}
	<slot {builder} {attrs} />
{:else}
	<svelte:element this={level} {...builder} use:builder.action {...$$restProps} {...attrs}>
		<slot {builder} {attrs} />
	</svelte:element>
{/if}
