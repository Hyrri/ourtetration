import { SvelteComponent } from "svelte";
import type { FormPath } from 'sveltekit-superforms';
import type { FieldsetProps } from './types.js';
declare class __sveltets_Render<T extends Record<string, unknown>, U extends FormPath<T>> {
    props(): FieldsetProps<T, U>;
    events(): {} & {
        [evt: string]: CustomEvent<any>;
    };
    slots(): {
        default: {
            fieldsetAttrs: {
                'data-fs-fieldset': string;
            };
            value: unknown;
            errors: string[];
            tainted: boolean;
            constraints: Partial<{}> | undefined;
        };
    };
}
export type FieldsetProps_<T extends Record<string, unknown>, U extends FormPath<T>> = ReturnType<__sveltets_Render<T, U>['props']>;
export type FieldsetEvents<T extends Record<string, unknown>, U extends FormPath<T>> = ReturnType<__sveltets_Render<T, U>['events']>;
export type FieldsetSlots<T extends Record<string, unknown>, U extends FormPath<T>> = ReturnType<__sveltets_Render<T, U>['slots']>;
/**
 * ## Fieldset
 * A component that groups related form controls or fields and extends the [Field](https://formsnap.dev/docs/components/field) component.
 *
 * - [Fieldset Documentation](https://formsnap.dev/docs/components/fieldset)
 * - [Field Documentation](https://formsnap.dev/docs/components/field)
 *
 * ### Slot Props
 * - `value` - The value of the field.
 * - `errors` - The errors of the field.
 * - `tainted` - The tainted state of the field.
 * - `constraints` - The constraints of the field.
 * - `fieldsetAttrs` - A spreadable object of attributes for the fieldset element if `asChild` is `true`.
 *
 * @param {SuperForm} form - The form object.
 * @param {FormPath<T>} name - The name of the field.
 * @param {boolean} [asChild=false] - Whether to opt out of rendering the fieldset element. [[asChild Docs](https://formsnap.dev/docs/composition/aschild)]
 */
export default class Fieldset<T extends Record<string, unknown>, U extends FormPath<T>> extends SvelteComponent<FieldsetProps_<T, U>, FieldsetEvents<T, U>, FieldsetSlots<T, U>> {
}
export {};
